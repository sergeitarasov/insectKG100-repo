# Auto detect text files and perform LF normalization
* text=auto

# Git LFS — large binary files
source_ontologies/*.owl    filter=lfs diff=lfs merge=lfs -text
kb/*.ttl                   filter=lfs diff=lfs merge=lfs -text
outputs/**/*-kb.ttl        filter=lfs diff=lfs merge=lfs -text
